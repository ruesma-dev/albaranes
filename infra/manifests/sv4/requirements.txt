# requirements.txt — sv4 (portal de revision humana, FastAPI + Jinja2)
# Paquete comun: se instala en el Dockerfile (COPY comun/ + pip install "/tmp/comun[azure]")
# que aporta azure-storage-queue + azure-identity para publicar en colas.
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9      # formularios (Form) de los endpoints del portal
jinja2>=3.1                  # plantillas del portal (Jinja2Templates)
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27                  # GraphTokenProvider (preview PDF) + Sigrid lookup
sqlalchemy>=2.0.30
psycopg[binary]>=3.1         # driver postgresql+psycopg (lectura del merge)
